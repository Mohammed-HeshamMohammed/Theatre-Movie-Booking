# Get a free API key at https://www.themoviedb.org/settings/api
# (the "API Key (v3 auth)" value, not the read access token).
# Without this set, the app falls back to a small bundled demo catalogue.
REACT_APP_TMDB_API_KEY=
